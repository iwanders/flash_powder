[package]
name = "torch_stable"
version = "0.1.0"
edition = "2024"
license = "BSD-3-Clause"

[lib]

[dependencies]
libc = {workspace=true}
anyhow = {workspace=true}
zerocopy = {workspace=true}

[build-dependencies]
cc={workspace=true}



[features]

# Whether to link against torch_cuda.
cuda = []

default=[ ]

[lints]
workspace = true
